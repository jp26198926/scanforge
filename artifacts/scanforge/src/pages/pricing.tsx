import { useState } from 'react';
import { useListPlans } from '@workspace/api-client-react';
import type { Plan } from '@workspace/api-client-react';
import { ArrowRight, Check, RefreshCw, ShieldCheck } from 'lucide-react';
import { PageIntro, primaryButton, secondaryButton } from '@/components/scanforge-shell';

const fallbackPlans: Plan[] = [
  { id: 'anonymous', name: 'Anonymous', price: 0, interval: 'none', dailyLimit: 1, description: 'Try one single entry without an account.', highlighted: false },
  { id: 'starter', name: 'Starter', price: 0, interval: 'none', dailyLimit: 3, description: 'Three daily transactions for single or bulk generation.', highlighted: false },
  { id: 'basic', name: 'Basic', price: 5, interval: 'monthly', dailyLimit: 50, description: 'Fifty daily transactions for growing teams.', highlighted: true },
];

export default function PricingPage() {
  const { data, isLoading, isError, refetch } = useListPlans();
  const [message, setMessage] = useState('');
  const plans = data && data.length > 0 ? data : fallbackPlans;
  return <div><PageIntro eyebrow="Capacity / 03" title="Room to run the line." detail="Start without an account. Upgrade when your queue gets serious — no surprise limits hiding behind the button." /><div className="mb-6 flex items-start gap-3 rounded-lg border border-secondary/50 bg-secondary/20 p-4 text-sm"><ShieldCheck className="mt-0.5 size-4 shrink-0 text-foreground" /><p><span className="font-semibold">All plans export the same crisp output.</span> <span className="text-muted-foreground">Plans change your daily capacity, not your code quality.</span></p></div>{isError && <div className="mb-6 flex items-center justify-between rounded-md border border-destructive/25 bg-destructive/5 px-4 py-3 text-xs text-destructive"><span>Live plan details are unavailable. Showing current standard tiers.</span><button type="button" onClick={() => refetch()} data-testid="button-retry-plans" className="font-semibold underline">Retry</button></div>}{isLoading ? <div className="grid gap-4 md:grid-cols-3">{[1, 2, 3].map((item) => <div key={item} className="h-80 animate-pulse rounded-lg border border-border bg-card" />)}</div> : <div className="grid gap-4 md:grid-cols-3">{plans.filter((plan) => plan.id !== 'admin').map((plan) => <article key={plan.id} className={`relative flex min-h-[335px] flex-col rounded-lg border bg-card p-6 shadow-sm ${plan.highlighted ? 'border-primary ring-1 ring-primary' : 'border-border'}`} data-testid={`card-plan-${plan.id}`}>{plan.highlighted && <span className="absolute -top-3 left-5 rounded-full bg-primary px-3 py-1 font-mono text-[10px] font-semibold uppercase tracking-[0.12em] text-primary-foreground">Most useful</span>}<div className="flex items-start justify-between"><div><h2 className="text-lg font-semibold">{plan.name}</h2><p className="mt-2 max-w-[200px] text-sm leading-5 text-muted-foreground">{plan.description}</p></div><span className="rounded-full bg-muted px-2 py-1 font-mono text-[10px] uppercase text-muted-foreground">{plan.dailyLimit}/day</span></div><div className="mt-8 flex items-baseline gap-1"><span className="text-4xl font-semibold tracking-[-0.05em]">{plan.price === 0 ? 'Free' : `$${plan.price}`}</span>{plan.price > 0 && <span className="text-xs text-muted-foreground">/ month</span>}</div><ul className="mt-7 space-y-3 text-xs text-muted-foreground"><li className="flex gap-2"><Check className="size-3.5 text-primary" /> High-resolution QR and barcode output</li><li className="flex gap-2"><Check className="size-3.5 text-primary" /> Single and bulk entry modes</li><li className="flex gap-2"><Check className="size-3.5 text-primary" /> Downloadable generation history</li></ul><button type="button" onClick={() => setMessage(plan.price === 0 ? 'You are already set up on the anonymous tier.' : `${plan.name} is ready for your workspace. Billing connection is coming next.`)} data-testid={`button-select-plan-${plan.id}`} className={`${plan.highlighted ? primaryButton : secondaryButton} mt-auto w-full`}>{plan.price === 0 ? 'Use free tier' : `Choose ${plan.name}`} {plan.price > 0 && <ArrowRight className="size-3.5" />}</button></article>)}</div>}{message && <div className="mt-6 flex items-center justify-between gap-4 rounded-md border border-primary/25 bg-primary/5 px-4 py-3 text-xs" data-testid="status-plan-selection"><span>{message}</span><button type="button" onClick={() => setMessage('')} className="text-muted-foreground underline" data-testid="button-dismiss-plan-message">Dismiss</button></div>}<p className="mt-8 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.14em] text-muted-foreground"><RefreshCw className="size-3" /> Limits reset daily at the time shown in your workspace</p></div>;
}